@model TaskManagementApp.Models.ApplicationUser

<div>
    <div class="card-body d-flex justify-content-between flex-column">
        <div>
        <p><i class="bi bi-person"> </i> UserName: @Model.UserName</p>

        <p><i class="bi bi-envelope"></i> Email utilizator: @Model.Email</p>

        @if (@Model.FirstName != null)
        {
            <p><i class="bi bi-person"> </i> Nume utilizator: @Model.FirstName</p>
        }

        @if (@Model.LastName != null)
        {
            <p><i class="bi bi-person"> </i> Prenume utilizator: @Model.LastName</p>
        }


        @if (@Model.PhoneNumber != null)
        {
            <p><i class="bi bi-phone"></i> Telefon utilizator: @Model.PhoneNumber</p>
        }
        </div>


        @if (ViewBag.EsteAdmin == true && ViewBag.AfisareButoane == true)
        {
            <div class="d-flex justify-content-center flex-row" style="gap:5px">

                <div> <a class="btn btn-sm btn-success" href="/Users/Edit/@Model.Id"> Editare utilizator</a> </div>

                <div> <a class="btn btn-sm btn-success" href="/Users/Show/@Model.Id"> Vizualizare utilizator</a> </div>

            </div>
        }
    </div>
</div>
